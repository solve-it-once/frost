langcode: en
status: true
dependencies: {  }
id: video
label: Video
description: "Upload your own mp4 video files rather than embedding from a third-party service. Should only be used for small videos, especially stripe backgrounds, where size and bandwidth aren't a concern."
source: video_file
queue_thumbnail_downloads: false
new_revision: true
source_configuration:
  source_field: field_media_video_file
field_map:
  name: name
  mimetype: field_media_mimetype
  filesize: field_media_filesize
